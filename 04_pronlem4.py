name = "Rohit is a  good boy  "

print(name.replace("  ", " ")) # one new string create and print
print(name) #name string not changed, this called immutablity of strings
#strings are immutable which means that you cannot change them by running functions of them

